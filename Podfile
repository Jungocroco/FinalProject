project 'Record Store Explore.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Record Store Explore' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Record Store Explore

  pod 'YelpAPI', '~> 2.0'
  pod 'BrightFutures'
end
